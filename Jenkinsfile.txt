pipeline {
    agent any

    environment {
        SONARQUBE_URL = 'http://10.0.2.15:9000'
        SONARQUBE_TOKEN = 'squ_912f8bb937d3622d051f807758ae7fc0d78befdb'
    }

    stages {
        stage('Checkout') {
            steps {
                // Checkout your code from the repository
                checkout scm
            }
        }

        stage('Build') {
            steps {
                script {
                    // Run Maven clean install and SonarQube analysis
                    withMaven(
                        maven: 'M3',  // Ensure Maven tool is installed and configured in Jenkins
                        options: [
                            // Optionally, you can specify additional properties for the SonarQube analysis
                            '-Dsonar.projectKey=my-app-key',
                            '-Dsonar.host.url=${SONARQUBE_URL}',
                            '-Dsonar.login=${SONARQUBE_TOKEN}'
                        ]
                    ) {
                        sh 'mvn clean install sonar:sonar'
                    }
                }
            }
        }

        stage('SonarQube Analysis') {
            steps {
                script {
                    // Trigger SonarQube scan if needed here, or it's handled by the Maven command
                    echo 'SonarQube analysis triggered.'
                }
            }
        }
    }

    post {
        success {
            echo 'Build and SonarQube analysis completed successfully.'
        }
        failure {
            echo 'Build or SonarQube analysis failed.'
        }
    }
}
